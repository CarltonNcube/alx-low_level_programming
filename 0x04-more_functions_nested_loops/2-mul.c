#include "main.h"

/**
 * mult -Multiplie two integers
 * @a: The first number to be multiplied 
 * @b: The second number to multiply with the first number
 * Return: The result of multiplication between a and b
 */
int mul(int a, int b)

{
	return (a * b);

}
